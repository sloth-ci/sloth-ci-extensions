listen_point: test

provider:
    dummy:
        message: test

extensions:
    dummy:
        # Use the module sloth_ci.ext.dummy.
        module: dummy
        
        #Some param. If missing, "baz" is used.
        foo: bar