listen_point: test

provider:
    dummy:
        message: test

extensions:
    dummy:
        # Use the module sloth_ci.ext.dummy_app.
        module: dummy_app
        
        #Some param. If missing, "baz" is used.
        foo: bar