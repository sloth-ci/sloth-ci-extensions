host: localhost
port: 8080

api_auth:
    login: admin
    password: password

paths:
    access_log: NUL
    error_log: NUL
    db: false

extensions:
    dummy:
        # Use the sloth_ci.ext.robots_txt module.
        module: dummy_server

        #Some param. If missing, "baz" is used.
        foo: bar